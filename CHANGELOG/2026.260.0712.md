# 2026.260.0712 — feat: author the `charly feature` command skill

## Summary

The `charly feature` command had no owning skill. Adds a `skill:` entity to the
`plugin-feature` candy (family declared in opencharly/pod-charly-hooks) documenting the
command's surface and when to use it. The skill is authored as a candy `skill:`
entity — the generated marketplace corpus is never hand-edited.

## How tested

`charly box validate` at the repo root against a charly binary (the repo's root
schema is newer than some pinned CI binaries; the manifest validates cleanly):
```
charly box validate: OK — checked 1 candy, 7 distros, 7 builders; 0 warnings, 0 errors
```

## Rulebook compliance

- Rule 1: this is the owning repo for the command plugin.
- R3: the skill documents the existing command surface; no second implementation.
- R5: additive only; no legacy path.

## Change classification

- Change class: candy skill entity (documentation).
- Verification gate: `charly box validate`.
- Attribution tier: `analysed on a live system` — the manifest validated live; the
  corpus projection is gated by the marketplace deploy drift gate after merge.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*